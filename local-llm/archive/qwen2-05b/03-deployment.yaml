apiVersion: apps/v1
kind: Deployment
metadata:
    name: qwen2-05b
    namespace: homelab-ai
spec:
    replicas: 1
    selector:
        matchLabels:
            app: qwen2-05b
    template:
        metadata:
            labels:
                app: qwen2-05b
        spec:
            nodeSelector:
                kubernetes.io/hostname: homelab-worker-01
            runtimeClassName: nvidia
            initContainers:
                - name: download-model
                  image: curlimages/curl:latest
                  command:
                      - sh
                      - -c
                      - |
                          MODEL=/models/qwen2-0_5b-instruct-q4_k_m.gguf
                          # validate existing file by checking GGUF magic bytes
                          if [ -f "$MODEL" ] && [ "$(dd if=$MODEL bs=1 count=4 2>/dev/null)" = "GGUF" ]; then
                            echo "Valid model already present, skipping download."
                          else
                            [ -f "$MODEL" ] && echo "Corrupt/incomplete model found, re-downloading..." && rm -f "$MODEL"
                            echo "Downloading Qwen2-0.5B-Instruct Q4_K_M..."
                            curl -L --fail -o "$MODEL" \
                              "https://huggingface.co/Qwen/Qwen2-0.5B-Instruct-GGUF/resolve/main/qwen2-0_5b-instruct-q4_k_m.gguf" \
                              && echo "Download complete." \
                              || { echo "Download failed"; rm -f "$MODEL"; exit 1; }
                          fi
                  volumeMounts:
                      - name: models
                        mountPath: /models
            containers:
                - name: llama-server
                  image: ghcr.io/ggerganov/llama.cpp:server-cuda-b4719
                  args:
                      - --model
                      - /models/qwen2-0_5b-instruct-q4_k_m.gguf
                      - --host
                      - 0.0.0.0
                      - --port
                      - "8080"
                      - --n-gpu-layers
                      - "99"
                      - --ctx-size
                      - "4096"
                  ports:
                      - containerPort: 8080
                  resources:
                      limits:
                          nvidia.com/gpu: "1"
                  volumeMounts:
                      - name: models
                        mountPath: /models
                  readinessProbe:
                      httpGet:
                          path: /health
                          port: 8080
                      initialDelaySeconds: 10
                      periodSeconds: 10
                  livenessProbe:
                      httpGet:
                          path: /health
                          port: 8080
                      initialDelaySeconds: 30
                      periodSeconds: 30
            volumes:
                - name: models
                  persistentVolumeClaim:
                      claimName: qwen2-05b-models-pvc
